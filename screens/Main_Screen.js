import { View, Text, StyleSheet } from 'react-native'
import React from 'react'

// this is the screen that will be shown to the user

const Main_Screen = () => {
  return (
    <View>
      <Text>Main_Screen</Text>
    </View>
  )
}

export default Main_Screen

const styles = StyleSheet.create({
    container: {
        color: "#fff",
    }
})