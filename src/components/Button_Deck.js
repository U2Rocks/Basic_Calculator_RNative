import { View, Text, StyleSheet} from 'react-native'
import React from 'react'

// this component is used to layout all the buttons neatly

const Button_Deck = () => {
  return (
    <View>
      <Text>Button_Deck</Text>
    </View>
  )
}

export default Button_Deck

const styles = StyleSheet.create({
    container: {
        color: "#fff",
    }
})